import React from 'react';

const ReadSecretComponent = (secretMessage) => {
    return (
        <div>
            <h1>{secretMessage}</h1>
        </div>
    );
}

export default ReadSecretComponent;
